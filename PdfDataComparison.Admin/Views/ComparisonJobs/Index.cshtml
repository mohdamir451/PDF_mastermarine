@model ComparisonJobsIndexVm
@{
    ViewData["Title"] = "PDF Comparison Jobs";
    ViewData["Subtitle"] = "Track job intake, status, and comparison readiness.";
}

<section class="page-hero">
    <div>
        <h2>PDF Comparison Jobs</h2>
    </div>
</section>

@await Html.PartialAsync("_IndexComparePanel")
@await Html.PartialAsync("_IndexSavedDataLink")
@await Html.PartialAsync("_IndexModals")

@section Styles {
    @await Html.PartialAsync("_IndexStyles")
}

@section Scripts {
    @await Html.PartialAsync("_IndexScripts")
}

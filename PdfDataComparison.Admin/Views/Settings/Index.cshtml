@{
    ViewData["Title"] = "Settings";
    ViewData["Subtitle"] = "Review platform configuration status.";
}

<section class="page-hero">
    <div>
        <h2>Settings</h2>
        <p>Configuration is managed through environment-specific app settings and deployment variables.</p>
    </div>
</section>

<section class="card form-card">
    <div class="empty-state">
        <div class="empty-state-icon">CFG</div>
        <h3>Runtime settings are read-only here</h3>
        <p>Use appsettings files, environment variables, or deployment configuration to change platform behavior. This screen is intentionally not editable until persisted admin settings are implemented.</p>
    </div>
</section>
